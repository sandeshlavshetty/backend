<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <h1 class="text-3xl tracking-tighter mb-3 font-serif text-center">All users</h1>
    <div class="w-full h-full bg-gradient-to-r from-blue-500 to-cyan-500 p-10 text-white mt-3 overflow-hidden">
        <div class="users flex flex-row flex-wrap justify-center gap-4">
            <div class="user max-w-72 p-4 bg-zinc-800 rounded-lg">
                <div class="w-full h-52 rounded-lg bg-red-500"></div>
                <h3 class="text-xl tracking-tighter mt-3">Harsh vandana sharma</h3>
                <h5 class="text-zinc-500">harsh@gmail.com</h3>
            </div>
        </div>
    </div>
    </div>
</body>
</html> -->


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="w-full h-full">
    <div class="nav bg-zinc-800 grid grid-cols-2  justify-items-stretch">
        <a class="text-blue-300 px-1 py-2 max-w-28" href="/">Back to home</a>
        <h1 class="text-3xl tracking-tighter  font-serif text-center text-white justify-self-start">All users</h1>
    </div>
    <div class="w-full min-h-screen bg-gradient-to-r from-blue-500 to-cyan-500 p-10 text-white mt-1 ">
        
        <div class="users flex flex-row flex-wrap justify-center gap-4">
            <% if(users.length > 0) { %>

                <% users.forEach(function(user){%>
                    <div class="user max-w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-52 rounded-lg bg-zinc-500 overflow-hidden">
                        <img class="w-full h-full object-cover" alt="image" src="<%= user.image %>"> </div>
                        <h3 class="text-xl tracking-tighter mt-3"><%= user.name  %></h3>
                        <h5 class="text-zinc-500"><%= user.email%></h3>
                            <div class="flex gap-3 mt-10">
                                <a class="text-zinc-200" href="/edit/<%= user._id %>">Edit User</a>
                                <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
                            </div>
                    </div>
                    <% }) %>

           <% }else{ %>

            <h3 class="text-zinc-500">No Users yet</h3>

          <% } %>

           
        </div>
    </div>
    </div>
</body>

</html>